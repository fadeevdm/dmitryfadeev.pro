const POSTS_URL='assets/posts.json';
const GISCUS={enable:false,repo:'owner/repo',repoId:'',category:'General',categoryId:'',mapping:'title',theme:'dark_dimmed',lang:'ru'};
function giscusWidget(title){if(!GISCUS.enable)return null;const s=document.createElement('script');s.src='https://giscus.app/client.js';s.setAttribute('data-repo',GISCUS.repo);if(GISCUS.repoId)s.setAttribute('data-repo-id',GISCUS.repoId);if(GISCUS.category)s.setAttribute('data-category',GISCUS.category);if(GISCUS.categoryId)s.setAttribute('data-category-id',GISCUS.categoryId);s.setAttribute('data-mapping',GISCUS.mapping);s.setAttribute('data-term',title);s.setAttribute('data-reactions-enabled','1');s.setAttribute('data-emit-metadata','0');s.setAttribute('data-theme',GISCUS.theme);s.setAttribute('data-lang',GISCUS.lang);s.crossOrigin='anonymous';s.async=true;return s;}
async function loadPosts(){try{const res=await fetch(POSTS_URL);const data=await res.json();const posts=data.posts||[];renderPostCards(posts);wireModal(posts);}catch(e){console.error(e);}}
function getLang(){return document.documentElement.lang||'ru';}
function renderPostCards(posts){const container=document.getElementById('posts');container.innerHTML='';const lang=getLang();posts.forEach((p,idx)=>{const card=document.createElement('article');card.className='card';const title=p[lang]?.title||p.ru?.title||'';const excerpt=p[lang]?.excerpt||'';const date=p.date||'';const img=p.cover||'';card.innerHTML=`${img?`<img src="${img}" alt="" style="width:100%;border-radius:12px;border:1px solid var(--border)">`:''}<h3>${title}</h3><p class="post-meta">${date}</p><p>${excerpt}</p><button class="btn" data-read-index="${idx}" data-i18n="readMore">Читать далее</button>`;container.appendChild(card);});if(typeof apply==='function')apply(getLang());}
function wireModal(posts){const modal=document.getElementById('postModal');const closeBtn=modal.querySelector('.modal__close');const body=document.getElementById('postBody');const titleEl=document.getElementById('postTitle');const metaEl=document.getElementById('postMeta');const giscusContainer=document.getElementById('giscus_container');document.getElementById('posts').addEventListener('click',e=>{const btn=e.target.closest('button[data-read-index]');if(!btn)return;const idx=+btn.getAttribute('data-read-index');const p=posts[idx];const lang=getLang();const content=(p[lang]?.content||'').replace(/\n/g,'<br>');titleEl.textContent=p[lang]?.title||p.ru?.title||'';metaEl.textContent=p.date||'';body.innerHTML=(p.images||[]).map(src=>`<img src="${src}" alt="" style="width:100%;border-radius:12px;border:1px solid var(--border);margin:8px 0">`).join('')+`<div style="margin-top:8px">${content}</div>`;giscusContainer.innerHTML='';const widget=giscusWidget(p[lang]?.title||p.ru?.title||'');if(widget)giscusContainer.appendChild(widget);modal.setAttribute('aria-hidden','false');});closeBtn.addEventListener('click',()=>modal.setAttribute('aria-hidden','true'));modal.addEventListener('click',e=>{if(e.target===modal)modal.setAttribute('aria-hidden','true')});}
document.addEventListener('DOMContentLoaded',loadPosts);document.querySelectorAll('.lang').forEach(b=>b.addEventListener('click',()=>setTimeout(loadPosts,0)));
